import { Dimensions } from 'react-native';

export * from './AppFontLoading';

export const SCREEN_WIDTH = Dimensions.get('window').width;
export const SCREEN_HEIGHT = Dimensions.get('window').height;
